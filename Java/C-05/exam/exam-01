선언적 함수
함수의 가장 일반적인 형태이다. 단 한 번만 파싱되며 페이지에서 찾아내기도 쉽고 이해하기도 쉽다. 게다가 자바스크립트를
지원하는 모든 브라우저에서 선언적 함수를 지원한다.

익명 함수
이름이 없으며, 변수를 배정받거나 함수 인자를 넘겨받는다. 그리고 접근될 때마다 파싱된다는 특징이 있다. 사용자 입력과
같은것을 처리할 경우에 유용하다.

리터럴 함수
객체에 대한 메소드를 정의 하거나 인자를 넘겨주는데 유용하다. 특히 이름을 주엇을때 내부에서만 사용가능 하므로
재귀에서 유용하다
