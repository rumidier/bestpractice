옵션
alwaysLowered
용도: '팝 언더' 윈도우라고도 한다. 부모 윈도우가 최소화 되는 경우를 제외하곤 항상 부모 윈도우 밑에 놓는다.
기본값: 기본값은 no다. UniversalBrowserWrite 권한이 필요하다.

옵션
alwaysRaised
용도: 부모 윈도우의 최상위에 윈도우를 연다.
기본값: 기본값은 no다. UniversalBrowserWrite 권한이 필요하다.

옵션
dependent
용도: 부모 윈도우에 종속된 윈도우를 연다. 부모 윈도우를 닫으면 같이 종료된다.
기본값: 기본값은 no다.

옵션
directories
용도: 개인 북마크 또는 링크 바를 표시한다.(구체적인 것은 브라우저 종류에 따라 다르다)
기본값: 기본값은 yes다. 일부 브라우저에서는 브라우저 자체 설정값이 우선한다.

옵션
height
용도: 컨텐트 영역의 가로 길이
기본값: 최소 100픽셀

옵션
width
용도: 컨텐트 영역의 가로 길이
기본값: 최소 100픽셀

옵션
outerHeight
용도: 전체 브라우저 윈도우의 세로 길이를 픽셀 단위로 표시
기본값: 최소 100픽셀

옵션
top
용도: 브라우저 윈도우의 제일 위쪽 위치 값
기본값: 화면 위에 표시 가능한 값이어여 한다

옵션
left
용도: 브라우저 윈도우의 제일 왼쪽 위치 값
기본값: 화면 위에 표시 가능한 값이어야 한다.

옵션
menubar
용도: 값이 yes이면 윈도우에 메뉴바를 표시한다
기본값: 일부 브라우저에서는 브라우저 자체 설정값이 우선한다

옵션
toolbar
용도: 값이 yes이면 윈도우에 툴바를 표시한다
기본값: 일부 브라우저에서는 브라우저 자체 설정값이 우선한다.

옵션
location
용도: 값이 yes이면 윈도우에 주소표시줄을 표시한다.
기본값: IE 7은 이 값의 설정 여부와 상관없이 항상 주소 표시줄이 화면에 표시된다.

옵션
status
용도: 값이 yes이면 윈도우 화면 하단에 상태 표시줄을 표시한다.
기본값: 일부 브라우저에서는 기본값이 yes로 되어 있다.

옵션
resizable
용도: 값이 yes이면 윈도우 크기를 변경할 수 있다.
기본값: 일부 브라우저에서는 브라우저 자체 설정값이 우선한다.

옵션
scrollbars
용도: 값이 yes이면 윈도우에 스크롤바를 표시(로드된 문서보다 윈도우가 작은 경우)한다.
기본값: 일부 브라우저에서는 브라우저 자체 설정값이 우선한다.

옵션
modal
용도: 모달 윈도우(이 윈도우를 닫아야만 메인 윈도우로 돌아갈 수 있다)를 연다.
기본값: 대화상자는 모달 윈도우다. 일부 브라우저에서는 UniversalBrowserWrite 권한이 필요하다.

옵션
dialog
용도: alert 윈도우와 외관과 동작이 유사한 대화상자를 연다.
기본값:

옵션
minimizable
용도: 값이 yes이면, 윈도우를 최소화하는 버튼을 추가한다.
기본값:

옵션
titlebar
용도: 제목 표시줄의 표시 여부를 결정한다.
기본값: 기본값은 on이다. UniversalBrowserWrite 권한이 필요하다. 잉ㄹ부 브라우저에서는 브라우저 자체 설정값이 우선한다.

옵션
close
용도: 닫기(close) 버튼의 표시 여부를 결정한다.
기본값: 기본값은 on이다. UniversalBrowserWrite 권한이 필요하다. 일부 브라우저 에서는 브라우저 자체 설정 값이 우선한다.

표 9-2

프로퍼티
hash
용도: http://some.com/somepage#somehash와 같은 URL에서 somehash 부분(즉, 해시마크(#) 뒤에 오는 문자열)을 가리킨다.

프로퍼티
host
용도: URL의 호스트명(도메인명)과 포트 번호

프로퍼티
hostname
용도: 호스트명(도메인명)

프로퍼티
href
용도: URL 전체

프로퍼티
pathname
용도: 도메인명 다음에 오는 경로명

프로퍼티
port
용도: 포트번호

프로퍼티
protocol
용도: 프로토콜(예를 들면, http)

프로퍼티
search
용도: 질의 문자열, URL의 첫번째 물음표(?) 다음에 오는 문자열을 가리킨다

프로퍼티
target
용도: URL의 target명
