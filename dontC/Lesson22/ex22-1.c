/*
 * 배열이 정말로 나란히 메모리 공간에 할당되는 자료형이 맞는가?
 * 우리는 배열에 대해서 다음과 같이 정의 하였다.
 * 배열은 동인한 자료형의 변수가 메모리 공간에 나란히 할당되는 형태의
 * 자료형이다."
 *
 * 여기서 중요한 것은 메모리 공간에 나란히 할당된다는 사실이다. 즉 다음과 같이 배열이 서언되면, 배열을 이루는 여섯 개의 변수가 모두 나란히 메모리 공간에 할당이 된단는 뜻이다.
 * int arr[2][3];
 *
 * 이번 문제는 이 사실의 진위 여부를 확인하기 위한 프로그램을 만드는 것이다. 여러분은 &연산자 를 이용해서 변수의 주소값을 구할 수 있다. 그렇다면 배
 * 열이 정말로 메모리 공간에 나란히 할당되는지를 직접 확인 하기 위한 예
 * 제를 작성해 보자
 */
#include <stdio.h>

int
main (int   argc,
      char *argv[])
{
}
